# Copy to .env (same folder as docker-compose.yml) and edit the paths.
# `docker compose` loads .env automatically - no need to set these manually
# per PowerShell session.
DATA_PATH=C:\Users\yourname\.hermes-docker
DATA_RO_PATH=C:\Users\yourname\.hermes-docker-readonly
DEV_PATH=C:\dev
